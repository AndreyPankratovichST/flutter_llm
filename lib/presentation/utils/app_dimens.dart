import 'package:flutter/material.dart';

final class AppDimens {
  // Spacing
  static const spacingXs = 4.0;
  static const spacingSm = 8.0;
  static const spacingMd = 10.0;
  static const spacingBase = 14.0;
  static const spacingLg = 16.0;
  static const spacingXl = 24.0;
  static const spacingXxl = 32.0;

  // Radii
  static const radiusSm = Radius.circular(4);
  static const radiusMd = Radius.circular(16);
  static const radiusLg = Radius.circular(24);

  // Icon / indicator sizes
  static const iconSizeLg = 48.0;
  static const indicatorSize = 20.0;
  static const indicatorStroke = 2.0;

  // Bubble
  static const bubbleMaxWidthFraction = 0.75;

  // Percentage
  static const percentMultiplier = 100;

  // Shadow
  static const shadowBlur = 4.0;
  static const shadowAlpha = 0.05;

  // Sheet handle
  static const sheetHandleAlpha = 0.4;
}
